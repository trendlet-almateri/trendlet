/**
 * Trendslet design system primitives. Import from this module so the
 * design DNA stays consistent across pages:
 *
 *   import { PageHeader, StatCard, StatusChip } from "@/components/system";
 *
 * Each primitive maps to a single visual pattern in the locked spec
 * (see /system demo route for a live reference).
 */

export { PageHeader } from "./page-header";
export { StatCard } from "./stat-card";
export { StatusChip, type StatusKey } from "./status-chip";
export { TabPills, type Tab } from "./tab-pills";
export { FilterBar, FilterSelect, FilterSubmit } from "./filter-bar";
export { DataTable, type Column } from "./data-table";
export { TopBar } from "./top-bar";
export { Card } from "./card";
export { UrgencyBadge } from "./urgency-badge";
